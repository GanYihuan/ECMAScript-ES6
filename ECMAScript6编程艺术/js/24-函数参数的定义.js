// 参数是一组有次序的值
function fun([x, y, z]) {
	// x = 100;
	// y = 200;
	// z = 300;
}
fun([100, 200, 300])

// 参数是一组无序的值
function run({ id, name, age }) {
	// id = '007';
	// name = 'Conan';
	// age = 28;
}
run({ id: '007', name: 'Conan', age: '28' })
